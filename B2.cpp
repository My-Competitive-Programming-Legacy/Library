#include<bits/stdc++.h>
using namespace std;

void solve() {
}

int main() {
#ifdef  _SHIT
    freopen("../in", "r", stdin);
    freopen("../out.txt", "w", stdout);
#endif
    cin.tie(0);
    cin.sync_with_stdio(0);

    int T;
    cin >> T;
    for (int i = 1; i <= T; i++) {
        cout << "Case #" << i << ": ";
        solve();
    }

    return 0;

}